package regexDemo1;

public class regexDemo7 {
    public static void main(String[] args) {
        /* 捕获分组
           正则内部使用：\\租号
           正则外部使用：$组号
         */

        /* 非捕获分组：分组后不需要再用本组数据，仅仅是把数据括起来。
            (?:) (?=) (?!) 都是非捕获分组
         */
    }
}
